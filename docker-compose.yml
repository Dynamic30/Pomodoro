services:
  pomo:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - pomo-data:/app/data
    environment:
      - DB_PATH=/app/data/pomodoro.db
    restart: unless-stopped

volumes:
  pomo-data: